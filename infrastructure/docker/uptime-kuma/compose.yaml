services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "${KUMA_BIND_ADDRESS:-127.0.0.1}:3001:3001"
    volumes:
      - /srv/data/appdata/uptime-kuma:/app/data
